version: "3.9"
services:
  api:
    build: ./apps/api
    ports:
      - "8000:8000"
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: nexus
      POSTGRES_PASSWORD: nexus
      POSTGRES_DB: nexusai
  redis:
    image: redis:7
  qdrant:
    image: qdrant/qdrant